#include <stdio.h>
#include <stdlib.h>

//Write a function that takes a string and returns its length.
int main()
{

        char firstName[10];
        scanf("%s", firstName);
        printf("%s", firstName);
        //printf("%s", mblen(firstName));


    return 0;
}
